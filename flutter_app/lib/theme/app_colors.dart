import 'package:flutter/material.dart';

class AppColors {
  // Primary – Violet
  static const primary       = Color(0xFF694CAB);
  static const primaryTint   = Color(0xFFEDE8F6);
  static const primaryTint2  = Color(0xFFD8CCF0);

  // Teal
  static const teal          = Color(0xFF2EB5A0);
  static const tealTint      = Color(0xFFE0F5F2);
  static const tealTile      = Color(0xFFCCEEEA);

  // Blue
  static const blue          = Color(0xFF4A90D9);
  static const blueTint      = Color(0xFFE5F1FB);
  static const blueTile      = Color(0xFFCCE4F7);

  // Green
  static const green         = Color(0xFF5BAE6B);
  static const greenTint     = Color(0xFFE8F5EB);
  static const greenTile     = Color(0xFFCCE8D2);

  // Neutrals
  static const bg            = Color(0xFFF5F4FA);
  static const white         = Color(0xFFFFFFFF);
  static const ink           = Color(0xFF1A1A2E);
  static const muted         = Color(0xFF8A8A9A);
  static const line          = Color(0xFFE8E8F0);
  static const placeholder   = Color(0xFFD8D8E8);
  static const stroke        = Color(0xFFCCCCDD);

  // Semantic
  static const error         = Color(0xFFE05C5C);
  static const success       = Color(0xFF5BAE6B);
  static const warning       = Color(0xFFE8A020);
}
